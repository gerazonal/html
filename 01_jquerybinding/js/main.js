$(function(){
    // 코드 작성
    // 버튼 click을 클릭하면 div박스의 내부색깔을 빨간색으로 변경

    // 버튼을 클릭하면
    $('button').click(function(){
        // 버튼을 클릭하면 div박스의 내부색을 빨강으로 변경
        $('div').css({"background":"red"})
    })
})